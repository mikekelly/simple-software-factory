[Unit]
Description=Mount the data disk and take the host's ssf binary, config and keys from the seed disk
DefaultDependencies=no
After=local-fs.target
Before=sshd.service herdr-server.service ssf.service

[Service]
Type=oneshot
ExecStart=/usr/local/lib/ssf/seed.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target

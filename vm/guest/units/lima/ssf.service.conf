# Drop-in under lima: see ssf-seed.service.conf. Without default dependencies the
# unit keeps only what it names, so the basics and a clean shutdown are
# spelled out here.
[Unit]
DefaultDependencies=no
After=basic.target
Conflicts=shutdown.target
Before=shutdown.target
